Generated TypeScript protobuf stubs can be placed here if the project later moves from dynamic proto loading to static code generation.

The current gateway intentionally loads `protos/translation.proto` at runtime to keep `/protos` as the single source of truth during the hackathon.
